<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.javabuilder.mybatis.mapper.UserMapper">

    <update id="update">
        UPDATE users
        <set>
            <if test="request.name != null">name = #{request.name},</if>
            <if test="request.email != null">email = #{request.email},</if>
            <if test="request.status != null">status = #{request.status}</if>
        </set>
        WHERE id = #{id}
    </update>

    <resultMap id="UserWithRoles" type="com.javabuilder.mybatis.model.User">
        <id property="id" column="user_id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <collection property="roles" ofType="com.javabuilder.mybatis.model.Role">
            <id property="id" column="role_id"/>
            <result property="name" column="role_name"/>
        </collection>
    </resultMap>

    <select id="findByEmail" resultMap="UserWithRoles">
        SELECT u.id       AS user_id,
               u.name,
               u.email,
               u.password,
               u.status,
               r.id       AS role_id,
               r.name     AS role_name
        FROM users u
                 LEFT JOIN user_roles ur ON u.id = ur.user_id
                 LEFT JOIN roles r ON r.id = ur.role_id
        WHERE u.email = #{email}
    </select>

</mapper>